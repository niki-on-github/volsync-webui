apiVersion: v1
kind: Service
metadata:
  name: volsync-webui
  namespace: volsync-webui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: volsync-webui